apply plugin: 'c'

model {
	components {
            archAArch64(NativeLibrarySpec) {
                targetPlatform "tinkerArm8aHardVfp"
                binaries.withType(SharedLibraryBinarySpec) {
                    buildable = false
                }
	        sources {
		        c {
		            lib project: ':base', library: 'base', linkage: 'static'
		            lib project: ':kernel', library: 'kernel', linkage: 'static'
		        }
	        }
	    }
	}
}
